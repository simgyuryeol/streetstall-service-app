#마리아 db
spring:
  datasource:
    url: jdbc:mariadb://street-stall-webserver.cjxaagbjsitm.ap-northeast-2.rds.amazonaws.com:3306/street_stall_webserver
    username: tlarbfuf
    password: rbfuf106203
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
        hibernate:
          # show_sql: true
          format_sql: true
logging.level:
  org.hibernate.SQL: debug
